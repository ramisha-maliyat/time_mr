import SimplePage from "@/components/SimplePage";

export default function DSTPage() {
  return (
    <SimplePage
      title="Daylight Saving Time"
      description="Daylight Saving Time changes clocks in some countries during certain months. TIME.MR will later show which locations are currently using DST."
    />
  );
}