import SimplePage from "@/components/SimplePage";

export default function HowToUsePage() {
  return (
    <SimplePage
      title="How to use TIME.MR"
      description="Click any city to see its live local time. Use the menu to open timer, countdown, calendar, UTC clock, Unix clock, and timezone converter tools."
    />
  );
}