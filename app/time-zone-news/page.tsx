import SimplePage from "@/components/SimplePage";

export default function TimeZoneNewsPage() {
  return (
    <SimplePage
      title="Time zone news"
      description="This section will later show updates about timezone changes, daylight saving updates, and global clock changes."
    />
  );
}